<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="index.css">
  <title>ayo this is the root</title>
</head>
<body>
    <h1>Root</h1>
    <form action="/createTest" method="POST">
        <fieldset>
            <legend>Model: Test</legend>
            <input type="text" placeholder="Thing" name="thing">
            <button type="submit">Submit</button>
        </fieldset>
    </form>
    <ul>
        <% for (let item of tests) { %>
            <li class="tests" data-id="<%=item._id%>"> <%= item.thing %></li>
        <% } %>
    </ul>

</body>
</html>